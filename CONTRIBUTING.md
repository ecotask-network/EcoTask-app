# Contributing to EcoTask App

## Prerequisites
- Node.js 18+
- React Native CLI
- Android Studio (Android) or Xcode (iOS)

## Setup
1. `cp .env.example .env`
2. `npm install`
3. `cd ios && pod install && cd ..` (Mac only)
4. `npm start`

## Testing
```bash
npm test
npm run lint
```
